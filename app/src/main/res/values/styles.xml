<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="android:textColor">@color/colorText</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <!-- Add other theme customizations here -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay"
        parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomNumberPickerStyle">
        <item name="android:showDividers">none</item> <!-- Remove horizontal divider -->
        <item name="android:textColor">@color/colorText</item> <!-- Selected text color -->
        <item name="android:textSize">18sp</item> <!-- Adjust text size as needed -->
        <!-- Add other styling attributes here -->
    </style>

</resources>